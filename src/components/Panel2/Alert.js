const Alert = () => {
    return (
        <div className="alert alert-danger text-center" role="alert">
            Please ensure all fields are filled in and selected
        </div>
    )
}

export default Alert
